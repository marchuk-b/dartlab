name: photo_editor
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  colorfilter_generator: ^0.0.8
  crop_image: ^1.0.16
  flutter:
    sdk: flutter
  image_picker: ^1.2.0
  provider: ^6.1.5+1
  screenshot: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
